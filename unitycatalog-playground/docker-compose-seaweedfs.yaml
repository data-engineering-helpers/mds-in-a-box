#
# File: https://github.com/data-engineering-helpers/mds-in-a-box/tree/main/unitycatalog-playground/docker-compose-seaweedfs.yaml
#
# See also:
# * Unity Catalog: https://github.com/data-engineering-helpers/mds-in-a-box/tree/main/unitycatalog-playground/docker-compose.yaml
#
# Inspired by: https://github.com/seaweedfs/seaweedfs/blob/master/docker/seaweedfs-compose.yml
#
# seaweedfs image:
# * On Docker Hub: https://hub.docker.com/r/chrislusf/seaweedfs
# * Dockerfile on GitHub: https://github.com/seaweedfs/seaweedfs/blob/master/docker/Dockerfile.e2e
#
name: lakehouse-sw

services:

  master:
    image: chrislusf/seaweedfs # use a remote image
    container_name: master
    ports:
      - 9333:9333
      - 19333:19333
      - 9324:9324
    command: 'master -ip=master -ip.bind=0.0.0.0 -metricsPort=9324'
    networks:
      - lhnet

  volume:
    image: chrislusf/seaweedfs # use a remote image
    container_name: volume
    ports:
      - 8060:8060
      - 18080:18080
      - 9325:9325
    command: 'volume -ip=volume -master="master:9333" -ip.bind=0.0.0.0 -port=8060 -metricsPort=9325'
    depends_on:
      - master
    networks:
      - lhnet

  filer:
    image: chrislusf/seaweedfs # use a remote image
    container_name: filer
    ports:
      - 8888:8888
      - 18888:18888
      - 9326:9326
    volumes:
      - type: bind
        source: ./etc/conf/s3.json
        target: /etc/seaweedfs/s3.json
    command: 'filer -s3.config=/etc/seaweedfs/s3.json -ip=filer -master="master:9333" -ip.bind=0.0.0.0 -metricsPort=9326'
    tty: true
    stdin_open: true
    depends_on:
      - master
      - volume
    networks:
      - lhnet

  s3:
    image: chrislusf/seaweedfs # use a remote image
    container_name: s3
    ports:
      - 8333:8333
      - 9327:9327
    volumes:
      - type: bind
        source: ./etc/conf/s3.json
        target: /etc/seaweedfs/s3.json
    command: 's3 -config=/etc/seaweedfs/s3.json -filer="filer:8888" -ip.bind=0.0.0.0 -metricsPort=9327'
    depends_on:
      - master
      - volume
      - filer
    networks:
      - lhnet

networks:
  lhnet:
    driver: bridge
